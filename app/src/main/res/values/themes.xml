<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ButtonMapper" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <!--
      Fully transparent background specifically - AppNotInstalledActivity
      shows only its AlertDialog, this theme keeps the underlying Activity
      window itself invisible so there's no empty/flashing background
      behind the dialog.
    -->
    <style name="Theme.ButtonMapper.Transparent" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
